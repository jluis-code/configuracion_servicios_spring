eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
server:
  port: 9000
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: rootpass
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/viajes
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
